/**
 * Middle-layer that implements the stages of the crawler's pipeline (see Specification).
 * It is the 'glue' between the top layer and the implementation. 
 * 
 * @author David Pasch
 */
package rpick.crawl.frame.stages;
